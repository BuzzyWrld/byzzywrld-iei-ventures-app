# Copy to .env.local and fill in as needed.
# Nothing required for Day 1 — SQLite + filesystem are local.

# Anthropic API (used by real Brand Playbook skill once handed off)
# ANTHROPIC_API_KEY=

# Vercel deploy API (Day 4 — landing page auto-deploy)
# VERCEL_TOKEN=
# VERCEL_TEAM_ID=
